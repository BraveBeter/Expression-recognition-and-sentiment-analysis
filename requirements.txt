torch>=1.7.0  # 用于深度学习模型构建与训练
torchvision>=0.8.1  # 通常与PyTorch配合使用，提供数据加载等工具（虽然代码未直接导入，但可能间接依赖）
numpy>=1.19.5  # 用于数值计算和数组操作
pandas>=1.1.5  # 用于数据处理（如读取CSV等，代码中虽未直接展示数据读取，但可能涉及）
opencv-python>=4.5.1  # 即cv2，用于图像处理（如人脸检测、图像缩放等）
matplotlib>=3.3.4  # 用于数据可视化（绘制图表等）
tkinter>=8.6  # 用于图形用户界面弹窗（messagebox）
python >= 3.9